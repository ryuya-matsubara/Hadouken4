{
  "name": "hadouken-online-backend",
  "version": "1.0.0",
  "private": true,
  "description": "Lambda handlers for Hadouken Battle online (WebSocket) mode",
  "main": "handlers/index.js",
  "comment": "The @aws-sdk/* v3 packages are provided by the Node.js 20.x/22.x Lambda runtime, so they are NOT bundled. They are listed under devDependencies only so the code can be run/tested locally.",
  "devDependencies": {
    "@aws-sdk/client-apigatewaymanagementapi": "^3.700.0",
    "@aws-sdk/client-dynamodb": "^3.700.0",
    "@aws-sdk/lib-dynamodb": "^3.700.0"
  }
}
